"""Deterministic facts validation: term policy, repairs, findings, gate.

Mirrors the split of ``ontocast/tool/facts_validation/`` -- ``term_policy``
and ``unit_findings`` for the per-unit lane, ``gate`` and ``gate_repairs``
for the document-level one, ``acceptance`` for what lets a render leave the
loop, and the loop/repair modules for the control flow around them.
"""
