"""The ontology lane: catalog identity, context assembly, delta validation.

``access``/``identity``/``manager``/``merge`` cover the catalog;
``context_mode``/``condense``/``prompt_context``/``snapshot_apply`` the
per-unit context; ``unit_findings``/``reconcile`` the deterministic
validation added alongside it; ``loop_telemetry``/``suggestions_lifecycle``
the render/critic loop.
"""
